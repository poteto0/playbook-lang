[workspace]
members = [
    "core",
    "cli",
    "linter",
    "formatter",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"

[profile.release.package.playbook-cli]
strip = true
